#include<stdio.h>
#include<stdlib.h>
#include "dataBase.h"

/* Drier program to test above function*/
int main()
{
    struct node *root = NULL;
 /*   insert(&root,5);
    insert(&root,3);
    insert(&root,7);
    insert(&root,2);
    insert(&root,4);
    insert(&root,6);
    insert(&root,8);
    insert(&root,11);
    printf("inorder Traversal Is : ");
    inorder(root);*/

    return 0;
}
